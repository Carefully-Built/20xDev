From 431f1ef288a30f6dad5bd7b4d6cf5c05900e3319 Mon Sep 17 00:00:00 2001
From: Modus <modusHelper@users.noreply.github.com>
Date: Tue, 31 Mar 2026 08:54:38 +0200
Subject: [PATCH 1/2] Harden WorkOS AuthKit Convex token refresh

---
 src/providers/workos-convex-auth.tsx | 19 ++++++++++++-------
 1 file changed, 12 insertions(+), 7 deletions(-)

diff --git a/src/providers/workos-convex-auth.tsx b/src/providers/workos-convex-auth.tsx
index a8b6946..d51561d 100644
--- a/src/providers/workos-convex-auth.tsx
+++ b/src/providers/workos-convex-auth.tsx
@@ -26,10 +26,13 @@ export function useWorkosConvexAuth(): {
     }
     return (await refresh()) ?? null;
   }, [refresh, refreshAuth]);
+  const getHasOrganizationTokenMismatch = useCallback((
+    token: string | undefined,
+    nextOrganizationId: string
+  ): boolean => getTokenOrganizationId(token) !== nextOrganizationId, []);
 
   useEffect((): void => {
-    const tokenOrganizationId = getTokenOrganizationId(accessToken);
-    if (!user || !organizationId || tokenOrganizationId === organizationId) {
+    if (!user || !organizationId || !getHasOrganizationTokenMismatch(accessToken, organizationId)) {
       pendingOrganizationId.current = null;
       setIsRefreshingOrganization(false);
       return;
@@ -49,7 +52,7 @@ export function useWorkosConvexAuth(): {
         }
         setIsRefreshingOrganization(false);
       });
-  }, [accessToken, authLoading, organizationId, refreshAuth, refreshOrganizationToken, tokenLoading, user]);
+  }, [accessToken, authLoading, getHasOrganizationTokenMismatch, organizationId, refreshAuth, refreshOrganizationToken, tokenLoading, user]);
 
   const fetchAccessToken = useCallback(async (
     args: { forceRefreshToken?: boolean } = {}
@@ -59,20 +62,22 @@ export function useWorkosConvexAuth(): {
     }
     try {
       if (organizationId) {
-        const tokenOrganizationId = getTokenOrganizationId(accessToken);
         const needsOrganizationRefresh = (args.forceRefreshToken ?? false)
-          || tokenOrganizationId !== organizationId;
+          || getHasOrganizationTokenMismatch(accessToken, organizationId);
         if (needsOrganizationRefresh) {
           return await refreshOrganizationToken(organizationId);
         }
       }
       const token = await getAccessToken();
-      return token ?? null;
+      if (token) {
+        return token;
+      }
+      return organizationId ? await refreshOrganizationToken(organizationId) : null;
     } catch (error: unknown) {
       console.error('Failed to get WorkOS access token for Convex:', error);
       return null;
     }
-  }, [accessToken, getAccessToken, organizationId, refreshOrganizationToken, user]);
+  }, [accessToken, getAccessToken, getHasOrganizationTokenMismatch, organizationId, refreshOrganizationToken, user]);
 
   return useMemo(() => ({
     isAuthenticated: Boolean(user),
-- 
2.50.1 (Apple Git-155)


From 9375f3139336b2280e4e0b7b0c1a1f34bed4b7c5 Mon Sep 17 00:00:00 2001
From: Modus <modusHelper@users.noreply.github.com>
Date: Tue, 31 Mar 2026 08:54:42 +0200
Subject: [PATCH 2/2] Remove redundant client item organization filtering

---
 src/hooks/filter-items-by-organization.ts | 16 ----------------
 src/hooks/item-scope.ts                   | 19 +++++++++++++------
 src/hooks/use-organization-items.ts       |  4 +---
 3 files changed, 14 insertions(+), 25 deletions(-)
 delete mode 100644 src/hooks/filter-items-by-organization.ts

diff --git a/src/hooks/filter-items-by-organization.ts b/src/hooks/filter-items-by-organization.ts
deleted file mode 100644
index 2baa32c..0000000
--- a/src/hooks/filter-items-by-organization.ts
+++ /dev/null
@@ -1,16 +0,0 @@
-import type { api } from '@convex/_generated/api';
-import type { FunctionReturnType } from 'convex/server';
-
-type ItemsResult = FunctionReturnType<
-  typeof api.functions.items.queries.listByOrganization
-> | undefined;
-
-export function filterItemsByOrganization(
-  items: ItemsResult,
-  organizationId: string | null | undefined
-): NonNullable<ItemsResult> {
-  if (!organizationId || !items) {
-    return [];
-  }
-  return items.filter((item) => item.organizationId === organizationId);
-}
diff --git a/src/hooks/item-scope.ts b/src/hooks/item-scope.ts
index f37685c..ada46f3 100644
--- a/src/hooks/item-scope.ts
+++ b/src/hooks/item-scope.ts
@@ -9,15 +9,22 @@ export function requireOrganizationId(
   return organizationId;
 }
 
+export function getOrganizationQueryArgs(
+  organizationId: ScopedOrganizationId
+): { organizationId: string } | 'skip';
 export function getOrganizationQueryArgs<T extends Record<string, unknown>>(
   organizationId: ScopedOrganizationId,
-  args?: T
-): ({ organizationId: string } & T) | 'skip' {
+  args: T
+): ({ organizationId: string } & T) | 'skip';
+export function getOrganizationQueryArgs(
+  organizationId: ScopedOrganizationId,
+  args?: Record<string, unknown>
+): Record<string, unknown> | 'skip' {
   if (!organizationId) {
     return 'skip';
   }
-  return {
-    organizationId,
-    ...args,
-  } as { organizationId: string } & T;
+  if (!args) {
+    return { organizationId };
+  }
+  return { ...args, organizationId };
 }
diff --git a/src/hooks/use-organization-items.ts b/src/hooks/use-organization-items.ts
index 02f817b..b1c615d 100644
--- a/src/hooks/use-organization-items.ts
+++ b/src/hooks/use-organization-items.ts
@@ -1,4 +1,3 @@
-import { filterItemsByOrganization } from './filter-items-by-organization';
 import { useCreateItem, useDeleteItem, useUpdateItem } from './use-item-mutations';
 import { useItemsByOrganization } from './use-items';
 
@@ -24,13 +23,12 @@ export function useOrganizationItems(
   organizationId: string | null | undefined
 ): OrganizationItemsResult {
   const queryResult = useItemsByOrganization(organizationId);
-  const items = filterItemsByOrganization(queryResult, organizationId);
   const createItem = useCreateItem(organizationId);
   const updateItem = useUpdateItem(organizationId);
   const deleteItem = useDeleteItem(organizationId);
 
   return {
-    items,
+    items: queryResult ?? [],
     isLoading: Boolean(organizationId) && queryResult === undefined,
     createItem,
     updateItem,
-- 
2.50.1 (Apple Git-155)

